{
  "name": "repo-info",
  "description": "Function that takes a path to a local repository and returns an information object.",
  "version": "1.0.0",
  "dependencies": {
    "execa": "^0.4.0",
    "parse-github-url": "^0.3.1"
  },
  "devDependencies": {
    "ava": "^0.15.2",
    "os-tmpdir": "^1.0.1"
  },
  "files": [
    "index.js"
  ],
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "ava test.js",
    "postinstall": "rm -rf fixtures && git clone https://github.com/sindresorhus/lpad.git fixtures/lpad"
  }
}
